[package]
name = "xml-engine-traits"
version = "0.1.0"
edition = "2021"
authors = ["x-rs contributors"]
description = "Core trait abstractions for XML processing engines"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
